server.port= 8081
kafka.topic = iot-data

## Database Properties
spring.datasource.url = jdbc:mysql://localhost:3306/clinicallydb?allowPublicKeyRetrieval=true&useSSL=false
##spring.datasource.url = jdbc:mysql://localhost:3306/iotanalytics?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username = shivaraj
spring.datasource.password = shivaraj@123


## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

spring.datasource.driver-class-name=com.mysql.jdbc.Driver

# JPA properties
spring.jpa.show-sql=true